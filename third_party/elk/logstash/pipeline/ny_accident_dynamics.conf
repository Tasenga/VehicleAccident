input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.11.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://vehicleaccidents.c1rezrsxnuwk.us-east-2.rds.amazonaws.com/vehicleaccidents"
        jdbc_user => "master"
        jdbc_password => "mastervehicleaccidents"
        jdbc_validate_connection => true
        statement => 	"SELECT ny_neighborhood, ny_crash_datetime_per_month,
								(number_of_accidents)-(LAG(number_of_accidents,1) OVER(ORDER BY ny_neighborhood, ny_crash_datetime_per_month)) as ny_delta_number_of_accidents
						FROM
							(SELECT neighborhood as ny_neighborhood,
									to_char(crash_datetime,'YYYY-mm-28') as ny_crash_datetime_per_month,
									count(*) as number_of_accidents
							FROM accidents
							WHERE city='New York' AND neighborhood is not null
							GROUP BY neighborhood, ny_crash_datetime_per_month) as sub_select;"
    }
}

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    manage_template => true
    index => "ny_accident_dynamics"
	document_id => "%{ny_neighborhood}-%{ny_crash_datetime_per_month}"
  }
  stdout { codec => json_lines }
}
