input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.11.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://vehicleaccidents.c1rezrsxnuwk.us-east-2.rds.amazonaws.com/vehicleaccidents"
        jdbc_user => "master"
        jdbc_password => "mastervehicleaccidents"
        jdbc_validate_connection => true
        statement => "SELECT accidents.type_of_weather, ROUND(CAST((accidents.ny_number_of_accidents/(weather_duration_ny.duration_min/60)) as numeric), 2) as number_of_accidents_per_hour
						FROM
						(SELECT weather as type_of_weather, count(*) as ny_number_of_accidents FROM accidents GROUP BY weather) as accidents
						INNER JOIN
						weather_duration_ny
						ON accidents.type_of_weather = weather_duration_ny.weather;"
    }
}

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    manage_template => true
    index => "ny_weather"
  }
  stdout { codec => json_lines }
}

