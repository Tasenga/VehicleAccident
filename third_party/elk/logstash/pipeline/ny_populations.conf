input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.11.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://vehicleaccidents.c1rezrsxnuwk.us-east-2.rds.amazonaws.com/vehicleaccidents"
        jdbc_user => "master"
        jdbc_password => "mastervehicleaccidents"
        jdbc_validate_connection => true
        statement => 	"SELECT
						accidents.neighborhood as ny_neighborhood_with_population,
						sum(proc_population_ny.population) as ny_population,
						sum(accidents.number_of_accidents) as ny_number_of_accidents_per_neigborhood_with_population
						FROM
						(SELECT neighborhood, count(*) as number_of_accidents FROM accidents WHERE city='New York' AND neighborhood is not null GROUP BY neighborhood) as accidents
						LEFT OUTER JOIN proc_population_ny
						ON position(accidents.neighborhood in proc_population_ny.neighborhood)>0
						GROUP BY accidents.neighborhood"
    }
}


output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    manage_template => true
    index => "ny_populations"
  }
  stdout { codec => json_lines }
}
