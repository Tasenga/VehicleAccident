input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.11.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://vehicleaccidents.ci1cdsczdohb.us-east-2.rds-preview.amazonaws.com/vehicleaccidents"
        jdbc_user => "master"
        jdbc_password => "adminvehicleaccidents"
        jdbc_validate_connection => true
        statement => "SELECT borough, count(*) FROM accidents GROUP BY borough"
    }
}



output {
    stdout { codec => json_lines }
}
