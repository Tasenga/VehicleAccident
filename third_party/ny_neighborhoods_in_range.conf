input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.11.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://vehicleaccidents.c1rezrsxnuwk.us-east-2.rds.amazonaws.com/vehicleaccidents"
        jdbc_user => "master"
        jdbc_password => "mastervehicleaccidents"
        jdbc_validate_connection => true
        statement => 	"SELECT
						temp.ny_neighborhood_in_range as ny_neighborhood_in_range,
						temp.ny_number_of_accidents_per_neigborhood_in_range,
						ROW_NUMBER () OVER (ORDER BY temp.ny_number_of_accidents_per_neigborhood_in_range) as dangerous_range
						FROM (SELECT neighborhood as ny_neighborhood_in_range, count(*) as ny_number_of_accidents_per_neigborhood_in_range
								FROM accidents
								WHERE city='New York'
								GROUP BY neighborhood
								ORDER BY ny_number_of_accidents_per_neigborhood_in_range) as temp"
    }
}

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    manage_template => true
    index => "ny_neighborhoods_in_range"
  }
  stdout { codec => json_lines }
}

